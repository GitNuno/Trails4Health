@model IEnumerable<Trails4Health.Models.Opcao>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutBackoffice";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Questao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Questao.QuestaoID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OpcaoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OpcaoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OpcaoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
