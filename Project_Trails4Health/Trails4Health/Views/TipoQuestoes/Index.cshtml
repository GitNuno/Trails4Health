@model IEnumerable<Trails4Health.Models.TipoQuestao>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutBackoffice";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TipoQ)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoQ)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoQuestaoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoQuestaoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoQuestaoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
